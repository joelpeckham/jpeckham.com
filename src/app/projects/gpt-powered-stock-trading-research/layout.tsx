import { createProjectLayout } from "@/components/article-shell";

const { metadata, Layout } = createProjectLayout(
  "gpt-powered-stock-trading-research",
);
export { metadata };
export default Layout;
