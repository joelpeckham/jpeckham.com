**Card** — bordered surface with a hard offset shadow. Use for project tiles, notes, contact blocks.

```jsx
<Card accent="var(--red)" interactive>
  <h3>Project title</h3>
  <p>Short description.</p>
</Card>
```

`accent` adds a colored top-bar; `interactive` lifts the card on hover. Body padding is built in.
