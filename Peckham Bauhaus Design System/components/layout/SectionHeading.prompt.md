**SectionHeading** — the brand's big graphic section title: numbered badge + mono eyebrow + heavy ink rule under a black uppercase headline.

```jsx
<SectionHeading index="02" eyebrow="Selected work" title="Projects" accent="var(--blue)" />
```

Pair one per major page section. `accent` tints the badge; `align` left or center.
