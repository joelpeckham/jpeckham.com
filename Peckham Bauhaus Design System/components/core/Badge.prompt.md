**Badge** — filled block marker for section numbers ("01") or short status flags.

```jsx
<Badge variant="red">01</Badge>
<Badge variant="blue" shape="circle">✱</Badge>
```

Variants: `red`, `blue`, `yellow`, `ink`. `shape`: `square` (default) | `circle`.
