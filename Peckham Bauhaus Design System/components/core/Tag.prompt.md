**Tag** — compact mono chip for skills, tech, or keywords.

```jsx
<Tag>TypeScript</Tag>
<Tag variant="red">Featured</Tag>
```

Variants: `outline` (default), `ink`, `red`, `blue`, `yellow`. Group several with a flex row + gap.
