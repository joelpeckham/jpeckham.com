**NavBar** — portfolio top bar: dot + wordmark left, mono uppercase links right, heavy ink bottom rule.

```jsx
<NavBar brand="JOEL PECKHAM" items={["Work","About","Contact"]} active="Work" />
```

Presentational; wire the anchors yourself. `active` tints and underlines the current item red.
