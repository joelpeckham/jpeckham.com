**ArrowLink** — inline mono link with a sliding arrow and an animated ink underline on hover.

```jsx
<ArrowLink href="/work" color="var(--red)">See all projects</ArrowLink>
```

Use for "read more" / navigation affordances. `color` drives both text and underline.
