**Input** — bordered single-line field with a mono uppercase label. Focus flips the border and drops a hard accent shadow.

```jsx
<Input label="Email" type="email" placeholder="you@studio.com" accent="var(--red)" />
```

Pass native input props (`type`, `placeholder`, `value`, `onChange`). See `Textarea` for multi-line.
