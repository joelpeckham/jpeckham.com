**Textarea** — multi-line field matching `Input`'s bordered, accent-focus treatment.

```jsx
<Textarea label="Message" rows={6} placeholder="Tell me about the project…" />
```

Vertically resizable. Same props as a native `<textarea>` plus `label` and `accent`.
